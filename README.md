# Oshell
OShell is a simplified Unix-like command-line interpreter developed as part of an Operating Systems project. The shell supports interactive, batch, and pipe modes, built-in commands (cd, exit, env, setenv, unsetenv, alias, path), command operators (;, &amp;&amp;, ||, &amp;), output redirection (>), environment variable expansion, and basic process management.
